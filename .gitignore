# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/
env/

# Node
node_modules/
package-lock.json
npm-debug.log
yarn-error.log

# User data - sensitive information
service/data/*.json            # All user data files
!service/data/.gitkeep         # But keep the directory

# API keys and secrets
.env
.env.local
*.key
secrets/
config/secrets.yaml

# Personal/test data
test_data/
personal/
private/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Project specific - Generated/unnecessary files
# NOTE: outputs/raw/ and outputs/pdf_index.json ARE REQUIRED - DO NOT EXCLUDE
outputs/ocr/                    # OCR output (can regenerate)
outputs/structured/             # Structured data (can regenerate)
data/diya/                     # Reference datasets (not needed for runtime)
data/kg/                       # Knowledge graph data (not needed for runtime)
kg/                            # Knowledge graph output (not needed for runtime)
data-inspector/                # Generated reports (not needed for runtime)
tools/data-inspector/report/   # Data inspector reports (not needed for runtime)

# Training & Models - VERY LARGE (6GB+)
training/                      # Training data and scripts (6GB+)
models/                        # Pre-trained models (2GB+)
*.pth                          # PyTorch model weights
*.bin                          # Binary model files
*.safetensors                  # Model tensors
*.gguf                         # GGUF model format

# Backup folders
Diet plans backup 1/
Diet plans backup 2/
*backup*/

# PDF source files - NOT needed for runtime (outputs/raw has the extracted text)
1/                             # Original PDF folder 1
2/                             # Original PDF folder 2
"Diet plans"/                  # Original diet plans folder
"New diet plan"/               # New diet plans folder

# Development/documentation files - NOT needed for runtime
notebooks/                     # Jupyter notebooks (development only)
docs/                         # Documentation (optional)
*.md                          # Markdown docs (keep README.md)
!README.md                    # But include main README
!SETUP.md                     # And setup instructions
*.docx                        # Word documents
*.xlsx                        # Excel files

# Testing/tools - NOT needed for runtime
tests/                        # Unit tests (optional for deployment)
tools/                        # Development tools (not needed)
scripts/download_*.py         # Download scripts (not needed if models excluded)

# Logs
*.log
logs/
